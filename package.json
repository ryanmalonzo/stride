{
	"name": "stride",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:web": "bun --filter @stride/web dev",
		"dev:api": "bun --filter @stride/api dev",
		"db:migrate": "bun --filter @stride/api db:migrate",
		"db:generate": "bun --filter @stride/api db:generate"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.6"
	}
}

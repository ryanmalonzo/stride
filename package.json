{
  "name": "stride",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun --filter @stride/web dev",
    "dev:api": "bun --filter @stride/api dev"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.6"
  }
}
